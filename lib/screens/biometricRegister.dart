import 'package:flutter/material.dart';

class BiometricRegister extends StatelessWidget {
  static String routeName='/biometricreg';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('hi'),
      ),
    );
  }
}
